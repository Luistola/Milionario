import { PipeConcursoPipe } from './pipe-concurso.pipe';

describe('PipeConcursoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeConcursoPipe();
    expect(pipe).toBeTruthy();
  });
});
